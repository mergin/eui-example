import { environment } from '../environments/environment';

export const GLOBAL = {
    appTitle: 'eUI-app',
    baseUrl: environment.apiBaseUrl,
    languages: ['en', 'fr'],
    defaultLanguage: 'en',
    i18nFolder: 'i18n'
};
